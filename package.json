{
  "dependencies": {
    "webpack": "=3.10.0"
  },
  "scripts": {
    "start": "yarn cleanup_assets && NODE_ENV=development node ./node_modules/webpack/bin/webpack.js --version",
    "cleanup_assets": "echo 'cleanup assets'",

     "build": "yarn cleanup_assets && NODE_ENV=production node ./node_modules/webpack/bin/webpack.js --version"
  }
}
